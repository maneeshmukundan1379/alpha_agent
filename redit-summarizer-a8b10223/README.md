# Redit Summarizer

This project was generated by Alpha Agent Builder.

## Summary
- Template: `chat_assistant`
- Provider: `Google Gemini`
- Model: `gemini-2.5-flash`
- Frontend: `gradio`
- Tools: `document_context, structured_output`
- File uploads: `disabled`

## Setup
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

If you run this agent through Alpha Agent Builder, the saved API keys are injected automatically at runtime.
If you run it manually outside the builder, export the required provider key in your shell first.

## Run
```bash
python app.py
```

## One-shot execution
```bash
python run_agent.py "Summarize the latest developer updates."
```
